{
  "name": "vgc-games",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm --filter @vgc/game-runner build",
    "preview": "pnpm --filter @vgc/game-runner preview"
  },
  "workspaces": [
    "games/*",
    "packages/*"
  ],
  "scripts": {
    "process-art": "node scripts/process-art.js"
  },
  "devDependencies": {
    "commander": "^11.1.0",
    "sharp": "^0.32.6"
  }
}
